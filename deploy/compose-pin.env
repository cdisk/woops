# Auto-written by compose service tls-init (deploy/ensure-gateway-tls.sh).
# Listed after .env so the pin always matches deploy/tls/gateway.crt.
# Safe to commit empty; value is refreshed on every compose up.
OPS_GATEWAY_TLS_SPKI_SHA256=
